# Interactive Line Chart - A/B Test Statistics Visualization

Интерактивный график для визуализации статистики A/B тестов с отображением conversion rate (процент конверсии) для различных вариаций.

##  Live Demo

Проект опубликован на GitHub Pages:  
**[https://internMichael-JS.github.io/kameleoon-interview-task/](https://michael-js-bel.github.io/kameleoon-interview-task/)**

---

##  Выбранная библиотека визуализации

**Recharts** (v3.4.1) — React-библиотека для построения графиков на основе D3.js.

**Почему Recharts:**
- Простая интеграция с React
- Гибкая настройка компонентов
- Поддержка TypeScript
- Responsive контейнеры
- Хорошая документация и активное сообщество

---

##  Реализованные функции

### Основные требования:

- [X] **Line Chart с Conversion Rate** — отображение процентного значения конверсии для всех вариаций
- [X] **Hover эффект** — при наведении показывается вертикальная линия и всплывающее окно (tooltip) с ежедневными данными
- [X] **Обязательный выбор вариации** — минимум одна вариация всегда выбрана (включая опцию "All variations")
- [X] **Автоматическая адаптация осей** — оси X и Y автоматически адаптируются к видимым данным при переключении вариаций
- [X] **Отображение в процентах** — все значения отображаются в процентном формате
- [X] **Responsive layout** — адаптивная верстка для экранов от 671px до 1300px
- [X] **Variations selector** — выбор отображаемых линий (вариаций)
- [X] **Day / Week selector** — переключение между дневным и недельным представлением данных

### Бонусные функции:

- [X] **Zoom / Reset zoom** — увеличение/уменьшение масштаба по оси Y с возможностью сброса
- [X] **Line style selector** — выбор стиля линии:
  - `Line` — линейный график
  - `Smooth` — сглаженная кривая (monotone)
  - `Area` — график с заливкой области
- [X] **Light / Dark theme** — поддержка темной темы через системные настройки (`prefers-color-scheme`). Реализовано через браузер тк нет кнопки в макете
- [X] **Export chart to PNG** — экспорт графика в PNG изображение

---

##  Технологический стек

- **React** 19.2.0
- **TypeScript** 5.9.3
- **Vite** 7.2.2 — сборщик и dev-сервер
- **Recharts** 3.4.1 — библиотека для графиков
- **recharts-to-png** 3.0.1 — экспорт графика в PNG
- **file-saver** 2.0.5 — сохранение файлов
- **CSS Modules** — стилизация компонентов

---

##  Инструкции по локальной настройке

### Предварительные требования

- **Node.js** версии 20 или выше
- **npm** 

### Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/internMichael-JS/kameleoon-interview-task.git
   cd kameleoon-interview-task
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

### Запуск проекта

3. **Запустите dev-сервер:**
   ```bash
   npm run dev
   ```

   Приложение будет доступно по адресу: `http://localhost:5173`

### Другие команды

- **Сборка для production:**
   ```bash
   npm run build
   ```
   Собранные файлы будут в папке `dist/`

- **Предпросмотр production сборки:**
   ```bash
   npm run preview
   ```

- **Проверка кода линтером:**
   ```bash
   npm run lint
   ```

- **Форматирование кода:**
   ```bash
   npm run format
   ```

---

##  Структура проекта

```
kameleoon-interview-task/
├── src/
│   ├── components/          # React компоненты
│   │   ├── ChartContainer/  # Контейнер графика
│   │   ├── ChartPanel/      # Панель с графиком
│   │   ├── ControlPanel/    # Панель управления
│   │   └── IconButton/      # Кнопка с иконкой
│   ├── hooks/               # Кастомные хуки
│   ├── data/                # Данные (data.json)
│   ├── styles/              # Глобальные стили
│   └── utils/               # Утилиты и хелперы
├── public/                  # Статические файлы
├── .github/workflows/       # GitHub Actions для деплоя
└── package.json
```

---

##  Особенности реализации

- **Расчет Conversion Rate:** `conversionRate = (conversions / visits) * 100`
- **Агрегация по неделям:** при выборе "Week" данные автоматически группируются по неделям
- **Динамический домен Y-оси:** автоматический расчет диапазона значений с учетом зума
- **Темная тема:** активируется автоматически при системных настройках `prefers-color-scheme: dark`

---

## 👤 Автор

Email: astapenko.m99@gmail.com
